% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{sassy_features}
\alias{sassy_features}
\title{Report Rsassy build and CPU feature information}
\usage{
sassy_features()
}
\value{
A named list of build, selected-backend, and CPU/runtime feature
values.
}
\description{
Returns diagnostic information about the selected Rsassy backend. Calling
this initializes the native backend if it has not already been loaded.
\code{rsassy_selected_backend} reports the runtime-selected backend.
\code{rsassy_installed_backends} is a character vector of backend libraries found
in the package installation, and \code{rsassy_supported_backends} is the subset
supported by the current CPU/runtime. With \code{"auto"} selection, Rsassy chooses
the best supported installed backend: AVX-512 before AVX2 on x86_64, NEON on
arm64, WebAssembly SIMD128 on wasm, and scalar otherwise. The \verb{selected_*}
fields describe the loaded Rust backend. The \verb{cpu_*} fields are detected by
the C shim.
}
\examples{
names(sassy_features())
}
