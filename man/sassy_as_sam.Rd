% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{sassy_as_sam}
\alias{sassy_as_sam}
\title{Format matches in SAM-compatible text direction}
\usage{
sassy_as_sam(x, alphabet = "dna")
}
\arguments{
\item{x}{A \code{sassy_matches} data frame.}

\item{alphabet}{Alphabet profile used for the search. One of \code{"dna"} or
\code{"iupac"} when \code{x} includes \code{match_region}.}
}
\value{
A copy of \code{x} with reverse-strand \code{cigar} values reversed and, when
present, reverse-strand \code{match_region} values reverse-complemented back to
text direction.
}
\description{
Rsassy normally follows the upstream \code{sassy} TSV convention: reverse-strand
\code{match_region} values are reverse-complemented and CIGAR strings are oriented
in the input pattern direction. \code{sassy_as_sam()} converts reverse-strand rows
to the text direction used by SAM and by upstream \code{sassy --sam} output.
}
\examples{
sassy_as_sam(
  sassy_search(list("ACGA"), list("TTTCGTTT"), 0, alphabet = "dna", match_region = TRUE),
  alphabet = "dna"
)
}
