% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{sassy_fastx_iter}
\alias{sassy_fastx_iter}
\title{Create a chunked FASTA/FASTQ iterator}
\usage{
sassy_fastx_iter(path, batch_records = 100000L, include_qual = TRUE)
}
\arguments{
\item{path}{Path to a FASTA/FASTQ file. Gzip-compressed input is supported by
the vendored \code{needletail} gzip backend.}

\item{batch_records}{Maximum number of records returned by each
\code{sassy_fastx_next()} call.}

\item{include_qual}{If \code{TRUE}, FASTQ qualities are included as \code{batch$qual}.
If \code{FALSE}, or for FASTA input, \code{batch$qual} is \code{NULL}.}
}
\value{
An external pointer with class \code{sassy_fastx_iter}.
}
\description{
\code{sassy_fastx_iter()} opens a FASTA or FASTQ file and returns an iterator that
yields record-count-bounded batches. Parsing is performed by the vendored
Rust \code{needletail} parser. Sequence and quality data in each batch are exposed
as read-only raw ALTREP slices over immutable native batch buffers; they are
not eagerly materialized as R strings.
}
\examples{
fq <- tempfile(fileext = ".fastq")
writeLines(c("@r1", "ACGT", "+", "!!!!"), fq, useBytes = TRUE)
it <- sassy_fastx_iter(fq, batch_records = 1)
batch <- sassy_fastx_next(it)
rawToChar(batch$seq[[1]])
}
