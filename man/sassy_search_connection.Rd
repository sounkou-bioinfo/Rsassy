% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{sassy_search_connection}
\alias{sassy_search_connection}
\title{Search an R connection with 'sassy'}
\usage{
sassy_search_connection(
  pattern,
  con,
  k,
  alphabet = "dna",
  rc = TRUE,
  alpha = NULL,
  all = FALSE,
  threads = 1L,
  mode = "single",
  chunk_size = 1024 * 1024,
  overlap = NULL,
  match_region = FALSE
)
}
\arguments{
\item{pattern}{Raw vector, character vector, or list of raw vectors / character scalars.}

\item{con}{An open readable R connection, preferably opened in binary mode.}

\item{k}{Maximum edit distance.}

\item{alphabet}{Alphabet profile. One of \code{"dna"}, \code{"iupac"}, or \code{"ascii"}.}

\item{rc}{If \code{TRUE}, search reverse-complement strand as well where supported.}

\item{alpha}{Optional IUPAC overhang cost in \verb{[0, 1]}. Use \code{NULL} to disable.}

\item{all}{If \code{FALSE}, return local-minimum matches. If \code{TRUE}, return all end positions with score <= \code{k}.}

\item{threads}{Number of worker threads to request for bulk searches.}

\item{mode}{Bulk search mode. \code{"single"} searches each pair independently; \code{"batch_texts"} uses one text per SIMD lane. \code{"batch_patterns"} and \code{"encoded_patterns"} (alias \code{"v2"}) use Sassy's multi-pattern encoding, which in \code{sassy} 0.2.1 is implemented for \code{alphabet = "iupac"} and equal byte-length patterns.}

\item{chunk_size}{Number of new bytes to read per native chunk.}

\item{overlap}{Number of bytes to carry from one chunk to the next. If \code{NULL}, C computes a default from the longest pattern, \code{k}, and \code{alpha}.}

\item{match_region}{If \code{TRUE}, include a \code{match_region} column. Reverse-strand
regions are reverse-complemented so the region and CIGAR are in the input
pattern direction.}
}
\value{
A data frame of matches with coordinates relative to the full stream.
}
\description{
Streams bytes from an already-open readable R connection through the C/R API
boundary. This avoids an R-level \code{readBin()}/\code{readChar()} loop while still
preserving matches that cross chunk boundaries by keeping an overlap window.
\code{pattern} may be a single sequence or a vector/list of sequences, with
\code{pattern_idx} identifying the 0-based input pattern index.
}
